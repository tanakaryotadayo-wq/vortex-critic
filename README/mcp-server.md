# MCP Server Module

`mcp-server/` は Fusion Orchestrator v2 を stdio MCP として公開するモジュールです。

## 含むもの

- `mcp-server/src/...`
- `mcp-server/start.sh`
- `mcp-server/package.json`

## 役割

1. Qwen / Gemini / Jules / n8n を MCP ツール化する
2. 外部クライアントから叩けるようにする
3. オーケストレーションを独立サーバーとして運用する

